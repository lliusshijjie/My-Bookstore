apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: tinywebserver
type: Opaque
# 使用 base64 编码: echo -n "root" | base64
# 生产环境请用 Sealed Secrets / Vault / External Secrets Operator 管理
stringData:
  MYSQL_ROOT_PASSWORD: "root"
  MYSQL_USER: "tinyuser"
  MYSQL_PASSWORD: "tinypass"
